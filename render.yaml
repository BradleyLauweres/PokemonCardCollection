services:
  - type: web
    name: pokemon-card-backend
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt && python manage.py migrate
    startCommand: gunicorn backend_project.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
